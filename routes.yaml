routes:
  /signup/: members/signup
  /signin/: members/signin
  /account/: members/account

collections:
  /:
    permalink: '/{year}/{month}/{slug}/'
    template: index

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
